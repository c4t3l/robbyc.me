+++
date = '2020-07-10T21:40:24-05:00'
title = 'Engineering Log 2020-07-10'
summary = 'Entry from my old private engineering blog -- CentOS 8 formula updates'
tags = ['centos', 'jenkins-ci', 'kitchen-ci']
+++


Centos 8 Formula Updates
------------------------

This project started on Monday (6/Jul/20) and the team is two formulas away from 
completing the "simple" formulas.  During the test phase of ops-minion, I found 
what looks to be a bug in kitchen-ci related to the `lifecycle` hooks.

```console
>>>>>> ------Exception-------
>>>>>> Class: Kitchen::UserError
>>>>>> Message: Unknown lifecycle hook target {}
```

Running checks on the kitchen versions of my workstation and the jenkins buildhost
showed a big discrepancy.  

```console
workstation: 2.5.1
buildhost: 1.23
```

Attempts to install a new buildhost were met with errors in deployment.  For example
the `at` package was recently removed from `ops-gpgks`, but it is also missing 
from the kickstart image in `img-instance-builds`.  

In addition, the `saltdk` installation failed to install `kitchen`.  I further 
checked the vm settings in vcenter and discovered that CPU passthru is locked
and I dont seem to have valid rights to enable it anymore.  This is incredibly
frustrating because it seems like my access rights change from week to week.
I recenty made this same change for `opsfed01` successfully so that Koji 
builds would work.  


RT Bridge
---------

Max ████████ called to discuss the new directions for RT that have been placed
upon him.  He relayed that Ram ██████ has added more services to the expected 
deployment of `rt-brdige` and now the scheduled cut over time will be 
missed.  Ram has to be repeatedly told our work methods.  At this point it 
is clear that he does not want to work with us to increase reliability.

